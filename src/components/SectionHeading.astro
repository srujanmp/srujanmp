---
interface Props {
  label: string;
  id?: string;
  class?: string;
}

const { label, id, class: className = '' } = Astro.props;
---

<div class={`rule-heading mb-8 ${className}`} id={id}>
  <h2 class="eyebrow !text-ink shrink-0">{label}</h2>
</div>
